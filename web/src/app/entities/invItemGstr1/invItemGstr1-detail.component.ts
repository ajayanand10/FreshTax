//
// Project home: https://github.com/jaxio/celerio-angular-quickstart
// 
// Source code generated by Celerio, an Open Source code generator by Jaxio.
// Documentation: http://www.jaxio.com/documentation/celerio/
// Source code: https://github.com/jaxio/celerio/
// Follow us on twitter: @jaxiosoft
// This header can be customized in Celerio conf...
// Template pack-angular:web/src/app/entities/entity-detail.component.ts.e.vm
//
import {Component, OnInit, OnDestroy, Input, Output, EventEmitter} from '@angular/core';
import { Router, ActivatedRoute } from '@angular/router';
import { SelectItem } from 'primeng/primeng';
import { MessageService} from '../../service/message.service';
import {InvItemGstr1} from './invItemGstr1';
import {InvItemGstr1Service} from './invItemGstr1.service';
import {InvGstr1} from '../invGstr1/invGstr1';

@Component({
    moduleId: module.id,
	templateUrl: 'invItemGstr1-detail.component.html',
	selector: 'invItemGstr1-detail',
})
export class InvItemGstr1DetailComponent implements OnInit, OnDestroy {
    invItemGstr1 : InvItemGstr1;

    private params_subscription: any;


    @Input() sub : boolean = false;
    @Input() // used to pass the parent when creating a new InvItemGstr1
    set inv(inv : InvGstr1) {
        this.invItemGstr1 = new InvItemGstr1();
        this.invItemGstr1.inv = inv;
    }

    @Output() onSaveClicked = new EventEmitter<InvItemGstr1>();
    @Output() onCancelClicked = new EventEmitter();

    constructor(private route: ActivatedRoute, private router: Router, private messageService: MessageService, private invItemGstr1Service: InvItemGstr1Service) {
    }

    ngOnInit() {
        if (this.sub) {
            return;
        }

        this.params_subscription = this.route.params.subscribe(params => {
            let id = params['id'];
            console.log('ngOnInit for invItemGstr1-detail ' + id);

            if (id === 'new') {
                this.invItemGstr1 = new InvItemGstr1();
            } else {
                this.invItemGstr1Service.getInvItemGstr1(id)
                    .subscribe(invItemGstr1 => {
                            this.invItemGstr1 = invItemGstr1;
                        },
                        error =>  this.messageService.error('ngOnInit error', error)
                    );
            }
        });
    }

    ngOnDestroy() {
        if (!this.sub) {
            this.params_subscription.unsubscribe();
        }
    }

    gotoInv() {
        this.router.navigate(['/invGstr1', this.invItemGstr1.inv.id]);
    }

    clearInv() {
        this.invItemGstr1.inv = null;
    }

    onSave() {
        this.invItemGstr1Service.update(this.invItemGstr1).
            subscribe(
                invItemGstr1 => {
                    this.invItemGstr1 = invItemGstr1;
                    if (this.sub) {
                        this.onSaveClicked.emit(this.invItemGstr1);
                        this.messageService.info('Saved OK and msg emitted', 'Angular Rocks!')
                    } else {
                        this.messageService.info('Saved OK', 'Angular Rocks!')
                    }
                },
                error =>  this.messageService.error('Could not save', error)
            );
    }

    onCancel() {
        if (this.sub) {
            this.onCancelClicked.emit("cancel");
            this.messageService.info('Cancel clicked and msg emitted', 'Angular Rocks!')
        }
    }

}
